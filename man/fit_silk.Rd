% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silk-method.R
\name{fit_silk}
\alias{fit_silk}
\title{Fit a SILK model}
\usage{
fit_silk(
  train_subjects,
  train_visits,
  shift_grid = NULL,
  seed = NULL,
  shift_range = NULL,
  method = "SILK",
  registration = NULL
)
}
\arguments{
\item{train_subjects}{Data frame of training subjects.}

\item{train_visits}{Data frame of training visits with biomarker columns.}

\item{shift_grid}{Numeric vector of candidate origin shifts. If omitted,
the grid is built from \code{shift_range} and \code{SHIFT_GRID_STEP}.}

\item{seed}{Integer or NULL. Random seed for reproducibility.}

\item{shift_range}{Numeric vector of length two giving the lower and upper
candidate shift values. Defaults to \code{DEFAULT_SHIFT_GRID_MIN} and
\code{DEFAULT_SHIFT_GRID_MAX}.}

\item{method}{Character label stored in prediction outputs.}

\item{registration}{Optional fitted object from
\code{fit_silk_registration}. Supplying it lets multiple survival layers
reuse exactly the same cross-fitted registration.}
}
\value{
A fitted SILK model object (list) for use with \code{predict_silk}.
}
\description{
Fits the SILK (Shift-Invariant Learned Kernel) model for absolute risk
prediction under origin-time measurement error. For real data, the error
distribution is not assumed known; the user supplies only a candidate shift
grid or range over which registration is optimized.
}
\examples{
\dontrun{
dat <- generate_dataset_fixed(200, "mean_moderate", seed = 1)
fit <- fit_silk(dat$subjects, dat$visits, shift_range = c(-12, 12), seed = 1)
}
}
